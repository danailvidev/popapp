//import * as services from './shared/services'

//const mapKeysToValueArrays = (obj) => Object.keys(obj).map(key => obj[key])

//export const serviceProviders = [
//    ...mapKeysToValueArrays(services)
//]